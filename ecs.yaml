imports:
    - { resource: 'vendor/wernerdweight/cs/src/ecs.yaml' }

parameters:
    skip:
        # this is a bug in Symplify (a bitwise operation is causing this)
        Symplify\CodingStandard\Sniffs\CleanCode\ForbiddenReferenceSniff:
            - 'TokenGenerator.php'
